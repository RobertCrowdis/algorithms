{
  "name": "algorithms",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm run build",
    "build": "rm -rf dist && tsc",
    "watch": "nodemon -e ts --watch src -x \"npm run build\"",
    "quick-sort": "npm run build && node dist/sorting/quick-sort.js",
    "merge-sort": "npm run build && node dist/sorting/merge-sort.js",
    "insertion-sort": "npm run build && node dist/sorting/insertion-sort.js",
    "bubble-sort": "npm run build && node dist/sorting/bubble-sort.js",
    "in-order-traversal": "npm run build && node dist/searching/tree-traversal.js --in-order",
    "pre-order-traversal": "npm run build && node dist/searching/tree-traversal.js --pre-order",
    "post-order-traversal": "npm run build && node dist/searching/tree-traversal.js --post-order",
  },
  "author": "Robert Crowdis",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^14.0.5",
    "nodemon": "^2.0.4",
    "typescript": "^3.9.3"
  }
}
