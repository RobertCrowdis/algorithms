{
  "name": "algorithms",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm run build",
    "build": "rm -rf dist && tsc",
    "watch": "nodemon -e ts --watch src -x \"npm run build\"",
    "quick-sort": "npm run build && node dist/quick-sort.js",
    "merge-sort": "npm run build && node dist/merge-sort.js",
    "insertion-sort": "npm run build && node dist/insertion-sort.js"
  },
  "author": "Robert Crowdis",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^8.0.26",
    "nodemon": "^1.12.1",
    "typescript": "^2.4.2"
  }
}
